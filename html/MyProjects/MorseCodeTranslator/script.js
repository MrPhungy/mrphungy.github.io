window.onload = function() {
    const outputElement = document.getElementById('output');
    outputElement.innerText = 'Translation'; // Default message
    outputElement.classList.add('default-message'); // Apply the default-message class
};
